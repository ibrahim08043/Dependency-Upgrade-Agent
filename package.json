{
  "name": "dependency-upgrade-agent",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "description": "AI-assisted dependency major-version upgrade agent: import a JS/TS repository, analyze its dependencies, run a real migration in an isolated workspace, and verify + approve the diff.",
  "workspaces": [
    "frontend",
    "backend",
    "shared/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n frontend,backend -c cyan,magenta \"npm:dev:frontend\" \"npm:dev:backend\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix frontend run typecheck",
    "test": "echo \"No project test suites are registered yet (see backend/tests + frontend/tests).\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}